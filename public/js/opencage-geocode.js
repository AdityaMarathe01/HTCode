// Stub for opencage-geocode.js — intentionally empty to avoid MIME errors
// Address lookups were removed; this stub prevents browsers from receiving HTML
// when they still request /js/opencage-geocode.js from cache.
// No global functions are exposed.
console.info('opencage-geocode.js stub loaded — address lookup removed');
